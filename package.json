{
  "name": "electrosnake",
  "version": "0.1.0",
  "description": "Electron App with Python backend",
  "main": "main.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && electron ./dist/main.js"
  },
  "author": "David Nies",
  "license": "ISC",
  "dependencies": {
    "typescript": "^3.2.4"
  },
  "devDependencies": {
    "electron": "^4.0.1"
  }
}
